{
  "name": "vaultify",
  "description": "Vaultify is a zero-knowledge secrets management platform for teams, projects, environments, and audit-safe secret sync.",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm --filter @vaultify/web run build",
    "build:all": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  }
}